# Uncomment the next line to define a global platform for your project
platform :ios, '14.0'

target 'AmountUsedExtension' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AmountUsedExtension
  pod 'Alamofire'
  pod 'SwiftSoup'
end

target 'TplusMobile' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TplusMobile
  pod 'Alamofire'
  pod 'SwiftSoup'
end
